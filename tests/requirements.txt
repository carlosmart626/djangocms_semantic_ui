# django-cms 5.1 imports `packaging` at runtime but does not declare it as a
# dependency; installing django-cms alone raises ModuleNotFoundError.
packaging>=23
Django>=5.2,<6.0
django-cms>=5.1,<5.2
coverage
flake8
